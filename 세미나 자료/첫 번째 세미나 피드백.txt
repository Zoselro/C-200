일정 -> 공유물 예약 관리 -> 자원 캘린더 -> 자원 예약 ->
사용자 : 회의에 참가하는 사람들
자원명 : 
예약명 : c++ 교육 세미나 저장

stl : 표준 템플릿 라이브러리 -> 헤더를 적용하면, 

stream : 데이터가 오고가고를 의미

iostream 안에 namespace가 있다는 의미

endl -> \rn

string 는 class 타입이다.

변수를 초기화 했다는게 아니라 할당을 했다라는 게 맞다. string은

string(1, ch) -> cplusplus 사이트 참고

for문은 순차 반복

for(initial state : condition ; increment / decrement){

}

배열이란 같은 형태의 자료형을 연속으로 특정 개수만큼 열거 해놓은 것.

founding[-1] = 1000; -> , out-of index로 오류

함수를 쓰는이유? 모듈화

const int x,y -> 함수 범위 내에서 상수처럼 사용하겠다.

namespace : 이름공간

java -> package 비스무리

namespace A{
 class C{

}
 class B{

}
 function{
}

static const int a = 60;
}

#include -> 선언한 함수, 클래스, 변수들을 사용하겠다.
헤더파일에 함수, 클래스, 변수, .... 를 사용하겠다.

전역변수 -> 특정 파일 안에서 프로그램이 실행 후 전 범위에서 사용 가능한 변수를 의미

헤더파일에서 extern int a = 10; 을 정의를 하게되면 다른 cpp에서도 쓸 수 있다.

A.compare(b) == 0

숙제)

문자열 관련된 내용 함수 외우기
std::string 관련 전부

+, ==      연산자 오버로딩(나중에 학습)

&& 연산자 ==> AND조건 (A AND B) A&&B
|| 연산자 ==> OR조건 (A OR B) A || B
^ 연산자 ==> XOR조건 (A XOR B) A ^ B
& 연산자 ==> AND조건 (A & B)
| 연산자 ==> OR조건 (A | B)

(CONDITION)이 true이면 A 실행 혹은 할당
condition이 false이면 B실행 혹은 할당

(condition) ? (statement1) : (statement2)

int a,b,c;

a도 integer, b도 integer, c도 integer 타입선언

int a = 10,

)A >> 4)